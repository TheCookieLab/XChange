on:
  schedule:
    - cron: '7 5 * * *'
  workflow_dispatch:
  push:
    branches: [ main ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: ${{ github.event_name == 'push' }}

jobs:
  build:
    name: Run integration tests
    permissions:
      actions: read
      contents: read
    uses: ./.github/workflows/it-template.yaml
    with:
      module-name: xchange-bitmex
